
	<% if @group %>
	  <div class="group">
	    <%= f.label :contact_info, t("activerecord.attributes.invite.contact_info", :default => "Contact Info"), :class => :label %>
	    <%= f.text_field :contact_info, :class => 'text_field' %>
	    <span class="description">Ex: a simple text</span>
	  </div>
	<% end %>

	<% if @contact %>
	  <div class="group">
	    <%= f.label :group_name, t("activerecord.attributes.invite.group_name", :default => "Group Name"), :class => :label %>
	    <%= f.text_field :group_name, :class => 'text_field' %>
	    <span class="description">Ex: a simple text</span>
	  </div>
	<% end %>
        
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  </button>
	<% cancel_path = group_invites_path if @group %>
	<% cancel_path = contact_invites_path if @contact %>
  <%= link_to "#{image_tag("web-app-theme/cross.png", :alt => "#{t("web-app-theme.cancel", :default => "Cancel")}")} #{t("web-app-theme.cancel", :default => "Cancel")}", cancel_path, :class => "button" %> 
</div>